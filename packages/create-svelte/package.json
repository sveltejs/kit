{
  "name": "create-svelte",
  "version": "2.0.0-alpha.7",
  "bin": "./bin",
  "devDependencies": {
    "gitignore-parser": "^0.0.2",
    "kleur": "^4.1.3",
    "prompts": "^2.3.2",
    "rollup": "^2.32.0",
    "tiny-glob": "^0.2.6"
  },
  "scripts": {
    "dev": "rollup -cw",
    "build": "rollup -c",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "template"
  ]
}

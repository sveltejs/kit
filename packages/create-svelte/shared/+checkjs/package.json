{
	"scripts": {
		"check": "svelte-kit sync && svelte-check",
		"check:watch": "svelte-kit sync && svelte-check --watch"
	},
	"devDependencies": {
		"typescript": "^4.9.3",
		"svelte-check": "^3.0.1"
	}
}

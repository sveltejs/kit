{
	"name": "test-build-errors",
	"private": true,
	"version": "0.0.0-next.0",
	"scripts": {
		"test": "vitest run",
		"test:cross-platform:build": "vitest run"
	},
	"type": "module",
	"devDependencies": {
		"vitest": "^2.1.6"
	}
}

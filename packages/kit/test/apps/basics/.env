PRIVATE_STATIC="accessible to server-side code/replaced at build-time"
PRIVATE_DYNAMIC="should not see me"

PUBLIC_STATIC="accessible anywhere/replaced at build-time"
PUBLIC_DYNAMIC="should not see me"
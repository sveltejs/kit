/**
 * Convert to folder-based routing system and adjust names for pages, layouts etc.
 *
 * @param {import('./types').BranchHierarchy} branch_hierarchy
 * @param {string} dest
 */
export function move_files(branch_hierarchy, dest) {
	// TODO
}

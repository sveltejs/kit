// we need this file to prevent vitest from crawling upwards and picking up a
// vitest config file from a different project

import { defineConfig } from 'vitest/config';

export default defineConfig({});

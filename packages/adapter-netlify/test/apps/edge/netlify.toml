[build]
publish = "build"

# TODO(serhalp): remove this when @netlify/dev supports serve/preview.
# @netlify/dev does not yet have explicit support for a "serve" mode, but this funky workaround is
# sufficient for our purposes for now. NOTE: do not do this in real apps; this is only for testing.
edge_functions = ".netlify/v1/edge-functions"

[[edge_functions]]
function = "sveltekit-render"
path = "/*"

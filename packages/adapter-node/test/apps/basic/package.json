{
	"name": "test-node-basic",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"dev": "vite dev",
		"build": "MY_CUSTOM_PORT=5173 INSTRUMENTATION_ENV=available vite build",
		"preview": "MY_CUSTOM_PORT=5173 INSTRUMENTATION_ENV=available node build",
		"test:build": "pnpm build && vitest run build.test.js",
		"test:platform": "playwright test"
	},
	"devDependencies": {
		"@playwright/test": "catalog:",
		"@sveltejs/kit": "workspace:^",
		"@sveltejs/vite-plugin-svelte": "catalog:",
		"svelte": "catalog:",
		"vite": "catalog:",
		"vitest": "catalog:"
	},
	"dependencies": {
		"server-side-dep": "file:server-side-dep"
	},
	"type": "module"
}

{
	"$schema": "https://turborepo.org/schema.json",
	"pipeline": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["files/**", "dist/**", "assets/**", ".svelte-kit/**"]
		},
		"check": {
			"outputs": []
		},
		"format": {},
		"test": {
			"dependsOn": ["^build", "$CI"],
			"outputs": []
		},
		"lint": {
			"dependsOn": ["format"],
			"outputs": []
		},
		"dev": {
			"cache": false
		}
	}
}

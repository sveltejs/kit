/* src/routes/crash-preload-client.svelte generated by Svelte v3.29.0 */
import {
	SvelteComponentDev,
	dispatch_dev,
	init,
	noop,
	safe_not_equal,
	validate_slots
} from "../../web_modules/svelte/internal.js";

const file = "src/routes/crash-preload-client.svelte";

function create_fragment(ctx) {
	const block = {
		c: noop,
		l: noop,
		m: noop,
		p: noop,
		i: noop,
		o: noop,
		d: noop
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_fragment.name,
		type: "component",
		source: "",
		ctx
	});

	return block;
}

async function preload() {
	if (typeof window !== "undefined") {
		throw new Error("Crashing now");
	}
}

function instance($$self, $$props, $$invalidate) {
	let { $$slots: slots = {}, $$scope } = $$props;
	validate_slots("Crash_preload_client", slots, []);
	const writable_props = [];

	Object.keys($$props).forEach(key => {
		if (!~writable_props.indexOf(key) && key.slice(0, 2) !== "$$") console.warn(`<Crash_preload_client> was created with unknown prop '${key}'`);
	});

	$$self.$capture_state = () => ({ preload });
	return [];
}

class Crash_preload_client extends SvelteComponentDev {
	constructor(options) {
		super(options);
		init(this, options, instance, create_fragment, safe_not_equal, {});

		dispatch_dev("SvelteRegisterComponent", {
			component: this,
			tagName: "Crash_preload_client",
			options,
			id: create_fragment.name
		});
	}
}

export default Crash_preload_client;
export { preload };
//# sourceMappingURL=crash-preload-client.js.map

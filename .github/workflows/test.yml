name: test

on:
  push:
    branches:
      - '*'

jobs:
  build:
    timeout-minutes: 6
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install dependencies
      run:
        npm i -g pnpm
        pnpm install

#    - name: Lint
#      run: pnpm lint

#    - name: Test
#      run: pnpm test

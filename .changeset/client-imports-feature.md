---
'@sveltejs/kit': minor
---

feat: add support for importing client module paths from server code

Allows server code to import client modules with `?client-import` query parameter, which returns the client-side path for dynamic loading

---
'@sveltejs/kit': minor
---

feat: clear remote form field dirty state when values are restored

Remote form field dirty() now reflects whether the current value differs from the value captured when the form was mounted.
